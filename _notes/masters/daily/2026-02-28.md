ok ok ok bro
torch `2.6.0` ????
try `Adam(fused=True)` and `torch.compile()`

ffffffuck, the `Triton` compiler requires cuda capabilities 7.0 and GTX 1080 Ti has 6.1
goddamn crusty-ass paleolitic GPUs
options for different backends - `aot_eager` or `cudagraphs` but they seem to be giving worse results than nothing
## datasets
#### VCTK
- adjust the noise floor empirically
- filter 0.1 - 1.0 seconds
    - handle "a", "i" later
- align if there's a delay at the start

do this in a script instead of running this in the notebook
#### laion 630k
query like
```
I'm processing textual descriptions of rather short sound recordings. Does the following snippet:
"<pasted description>"
describe a snare sound?
```