## experiments
nf - [[experiments - vctk words]]

mode collapse - should `_nf_latent_loss` be so negative?
oh my god is it the missing `+ 1e-4` again fuck
fucking starting from an even higher reconstruction loss after the first epoch fml

ok that didn't help, shit
it's negative that's weird, but then again the loss weight is so low it should pracitcally have no influence
[this impl](https://github.com/fmu2/flow-VAE/blob/main/static_flow_conv_vae.py) (`static_flow_conv_vae.py`) doesn't do the post-flow penalty term, just the log det + standard term forcing `z0 ~ N(0, 1)`
but the impl I used actually switches the first term to `q0.log_prob(z0)`, where `q0 = N(mean, scale)` and `z0 ~ q0`, why would this make sense???

ok fuck I don't trust this implementation, switch to `zuko`?
for `MaskedAffineFlow`s the log-det is just the `scale` network outputs masked + summed
then we do it for a couple of layers, 256 dims each, makes sense that the values just explode?
and dominate the logprobs because of course
???? fkn hell man
- [x] switch NF impl to `zuko`

![[Pasted image 20260531184947.png]]

no `MaskedAffineFlow` but there are `MaskedAutoregressiveFlow`s, go with that
## evals
_finding controllable directions_ rather than per-axis disentaglement

I need something that just runs the encoder on a bunch of samples
## rave repro
prior training once the run is complete
## thesis writing
I need to organize all my references goddamn
## next
- create the third last dataset - SFX or sum shi, from freesound?
- experiments - NF, [[RAVE]] prio
- ask the seminar guy/mamodrzejewski for direction
- thesis start planning out chapters
- gpu setup yeahhh idk