~ huh use https://kellerjordan.github.io/posts/muon/ ?
## experiments
NF go? oh boy
what combinations of prior+posterior? what sizes?

ok so generally it seems that `version_14` was the best config for [[experiments - vctk words]], with:
- `discriminator_lr: 5e-6`
- `feature_loss_weight: 20.0`
- `adversarial_loss_weight: 0.5`
- `encoder_training_len: 1000000`
- no adversarial loss warmups

NF experiments - try preserving the total parameter count
- go both prior/posterior at the same time for simplicity?
- better reconstructions with more parameters seems a bit silly/obvious, although just increasing capacity/latent space size didn't give this kind of result
- ~~ok nvm doing 16 layers both posterior prior increases model size by 0.8%, negligble~~
- ok nvm the `hidden_size=16` and 16 layers might not make sense
- try 4 layers of `latent_size == 256` parameters
- increase of 3%

fkn `nan`s from the encoder, def something with how gradients propagate through the flow then
- shi stopped failing after removing latent loss warmup huh
- does this actually make sense - the posterior was _too_ flexible?
## evals
disentaglement metrics - writing in [[experiments]]
for dimensionality reduction consider:
- PCA
- LDA - based on provided classes
- PLS - same thing
## rave repro
prior training once this run is done, although it does sound good now so
## next
- create the third last dataset - SFX or sum shi, from freesound?
- experiments - NF, [[RAVE]] prio
- ask the seminar guy/mamodrzejewski for direction
- thesis start planning out chapters
- gpu setup yeahhh idk