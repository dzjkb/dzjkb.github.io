## evals
got reconstructions saved from [[experiments - vctk words]] v17
the reconstruction loss is slightly lower than v13 and there's slightly more of the static high pitched constant noise in the audio
seems like the reconstruction loss is correlated with this noise? I don't really like that
and v13 had lower feature matching/adversarial/discriminator losses, eh
maybe no adversarial loss warmup then?

what next?
- FAD/KID/latents for val set reconstructions
- FAD/KID/latents for unconditional generations
- label examples with identity/sex/length, measure clustering quality of this labeling in the latent space
script this

detailing the "narrative" of the thesis in [[thesis goals]]

fucking `sudo poweroff`ed `gpu2` accidentally I swear to god lmaoo and it's not turning back on
## rave repro
restart after gpu2 crash from latest ckpt
```
rave train --config configs/jpconfig.gin --db_path /media/Data/jp_dir/sounds/vctk_rave --out_path /media/Data/jp_dir/sounds/rave_training --name vctk_words_v0 --channels 1 --workers 4 --val_every 4000 --max_steps 3000000 --gpu 1 --n_signal 131072 --ckpt /media/Data/jp_dir/sounds/rave_training/vctk_words_v0_0b2e4cd2e9/version_0/checkpoints/epoch_1500000.ckpt --batch 2
```

afterwards train prior -> calculate fad on random generations/reconstructions
## next
- thesis structure/narrative
- gpu setup