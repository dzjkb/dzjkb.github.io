yeah this asymmetric padding doesn't work, need to do it manually

right fuck
RAVE does it's own padding with `CachedConv1d`
which might explain the mismatch in upsampling/downsampling ratios
if the convs behave a bit differently w.r.t. dimensions than standard `torch.nn` ones