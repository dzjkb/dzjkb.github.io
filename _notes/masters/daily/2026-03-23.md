## experiments
[[experiments - vctk words]]
looks like it's actually working?? nice
but the conditional logic definitely slowed it down, unnecessary memory copies?
or maybe it's the memory trimming? but the worker processes get remade anyway, hm

different profiling:
```
profiler = PyTorchProfiler(
    schedule=torch.profiler.schedule(
        wait=100, warmup=2, active=5, 
        repeat=5  # Run 5 cycles instead of 1
    )
)
```

ok spent the whole day doing optimizations and looking at traces just for a 2% speedup what is this bs bruv

```
In case other users struggle to isolate the place where an operation or kernel is called from: you can enable Python backtraces in Nsight Systems allowing you to hover over each kernel and showing the backtraces through PyTorch’s C++ backend as well as your Python scripts.
```
https://apxml.com/courses/advanced-pytorch/chapter-4-deployment-performance-optimization/pytorch-profiler

it's the fucking `log` calls bro what
I can log every 50 batches why not f it

ok ok ok 170k steps into discriminator training the reconstruction loss fell to previous levels
and the generated samples started to sound the same as they did before discrimination on similar reconstruction loss levels
huh
I mean does make some sense, but I was hoping the different losses would get rid of the static
well, waiting
## architecture
ok why was `freeze_weights` a tensor in the first place in [[RAVE]]? for state? replacing with py value - optimizing to avoid GPU memory copies/syncs/type casts

add more `compile`s - feature matching/hinge gan losses
## studying
https://www.depthfirstlearning.com/2021/VI-with-NFs