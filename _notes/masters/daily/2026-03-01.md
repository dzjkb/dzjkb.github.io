ok ok nvm
`cudagraphs` works longer than the non-compiled versionu ughhhh
## datasets
#### VCTK
- adjust the noise floor empirically
- filter 0.1 - 1.0 seconds
    - handle "a", "i" later
- align if there's a delay at the start
wait but clips shorter than 1s probably don't have any silence at the start, so I can safely only filter
uhhhhhhhhhhhhhh
ok filtering f it
#### laion 630k
```
I'm processing textual descriptions of rather short sound recordings. Does the following snippet:
"<pasted description>"
describe a snare sound or something similar AND is a one-shot sound, not a loop? Answer with only a "yes" or "no".
```
https://docs.ollama.com/api/introduction
## experiments