https://github.com/Textualize/rich/issues/131
`pip install rich[jupyter]`
## datasets
#### VCTK
the ~0.1s samples sound good based on 40 random samples; start training
config - pretty much the same as `percussive1`
#### laion 630k
```
I'm processing textual descriptions of rather short sound recordings. Does the following snippet:
"<pasted description>"
describe a snare sound or something similar AND is a one-shot sound, not a loop? Answer with only a "yes" or "no".
```
working api usage in `laion630k.ipynb`

running filtering - is the sound a snare?
need some bg script for this shit instead of running in notebook + a smaller model? the ds is way too large for multiple queries like this
- 213.5min did 15736 rows (out of ~500k), waay too slow (73.7 rows per min, which gives 116 hours)
- which means I basically need a 100x faster model, try [qwen3:0.6b](https://ollama.com/library/qwen3)
705 samples are snares, inspect audio