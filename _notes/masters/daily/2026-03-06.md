~ training on @m10's server
~ buy the RTX 4060 Ti? gpu research in [[2026-02-27]]
~ someone bought the 1800pln RTX 4060 Ti already _fuck_
- [x] add `torch.compile()` if cuda compute capability permits
https://claude.ai/share/3548adae-6867-4116-a398-9653e21118af

sent email to zaikslab lmao lesgo
## datasets
#### laion 630k
ok `qwen2.5:14b` is taking a while, rich progress bar timing breaks
- create `laion_snares` ds from this
next up - try asking for percussive sounds in general
#### backup
should probably backup the important datasets in case I start putting too much strain on `gpu2`
also for transfer purposes
## experiments
#### VCTK
OOM - grads allocated during sample preprocessing?
- shouldn't happen
- `gc.collect()` doesn't help
- https://stackoverflow.com/questions/79464907/memory-keeps-increasing-in-pytorch-training-loop-even-with-empty-cache
- https://claude.ai/share/ef0f9396-5fc6-4507-ab96-a60fb79d5d20
ok main procecss uses constant memory, has to be the dataloaders
`UntypedStorage` - objects holding raw tensor bytes, bypassing python objects, `sys.sizeof()` reports only the struct size
- nvm it's not `UntypedStorage` using up the several gigs
ok fucking `scipy.signal.lfilter()`???
still leaks but way slower ok ok ok

removing `compose_left()` actually sped up training I swear to god lmao

ok `scipy` still doesn't release some random-ass buffers allocated via `malloc()`, freeing this seems to help:
```
def trim_memory() -> int:
    libc = ctypes.CDLL(ctypes.util.find_library("c"))
    return libc.malloc_trim(0) 
```
wtf man
oh my god it's the `persistent_workers=True`, otherwise the memory would et freed automatically with new worker processes
not sure if it's better to have this on or not though
## planning
[[experiments]] that is
4 datasets x NF/no-NF x reconstruction/generation
or should I combine `percussive1` and the freesound snares, probs yes
- although figuring out why `percussive1` sucks should provide valuable insights/uncover potential errors

next - dive into [[Crash]], start thesis content planning - [[thesis table of contents]], [[thesis goals]]
## model
would probably be a good idea to thinka moment about whether the single latent reduction isn't too simple - something RNN-like?
this per-dimension FC is hacky, bad sign in general

- [x] re-evaluate the single latent reduction method