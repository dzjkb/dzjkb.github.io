## experiments
[[experiments - vctk words]]

ok ok practically identical reconstruction losses to the best `v3` run 21 epochs in, but the latent loss is lower, that's nice
even despite the reversed dilations, hm

all losses except discriminator rising?
the most obvious explanation would be the discriminator winning too easily
the minimum might be over 0, not sure how the discriminator calculations influence that

ok shit the discriminator warmup might've been a bad idea afterall:
- if the discriminator is random the real/fake scores are very similar, which makes the gradient small
- same (probably?) for the features, making the feature matching loss small
- the generator has time to adapt to the adversarial training
in contrast to the situation after a warmup:
- the discriminator learns to discern the samples quite well
- the moment losses are turned on there's a massive gradient from adv and feature losses
- which, apparently, doesn't let the generator actually learn well

ideas:
- reduce discrmiminator lr
- reduce adversarial loss weight temporarily on a schedule
- remove discriminator warmup
ok did #1 and #3 for now