## experiments
gpu2 crashed, experiments on hold
## evals
ok ok ok
embedding actually works now thx Claude

1. (notebook) calculate FAD/KID on the embeddings
2. add FAD to validation runs during training (turn off any output though)

**unconditional generation - listening**
observations:
- very chaotic samples, all last the whole duration despite a majority of the training data being at most 0.5s
- sounds like multiple words are being mixed
- and multiple speakers
- but the sound is clearly voice
conclusions:
- disentaglement is probably poor - no single dimension for identity, length

what about the PCA dimensions though?
plot encoded validation set points after PCA + mark points with identity or length

**CLAP**
very clear distinction between the val set and the generated samples, shit, the fad probably sucks
maybe that's good though? room to show that introducing normalizing flows can improve this quality

**FAD**
ok so installing `fadtk` made the following changes
```
 + encodec==0.1.1
 + fadtk==1.0.1
 - huggingface-hub==1.6.0
 + huggingface-hub==0.36.2
 + hypy-utils==1.0.29
 - librosa==0.11.0
 + librosa==0.10.2.post1
 - llvmlite==0.46.0
 + llvmlite==0.41.1
 + msclap==1.3.4
 + nnaudio==0.3.4
 - numba==0.64.0
 + numba==0.58.1
 + resampy==0.4.3
 - soundfile==0.13.1
 + soundfile==0.12.1
 - transformers==5.3.0
 + transformers==4.57.6
 - wheel==0.46.3
 + wheel==0.41.3
```
hope that doesn't break anything

rewriting fad to use python bindings instead of the cli
ok ok

note: FAD and it's shortcomings is definitely something to write more about in the thesis
calculating FADInf because of the sample size bias thingy
ok works lesgo

disentaglment experiments?
_qualitative_: modifying the top PCA directions of a given sample
_quantitative_:
- clustering samples in PCA dimensions in the latent space
- trying to classify features (identity, sex, length) based on top PCA dimensions
moving to [[experiments]]

next:
- what were the FAD values for different relevant works?
- add [[Frechet Audio Distance]] to validation step
- add [[Kernel Inception Distance]] to validation step
## rave repro
restart after gpu2 crash from latest ckpt

afterwards train prior -> calculate fad on random generations
## local latex/thesis writing
citations bibtex

[fadtk paper](https://arxiv.org/pdf/2311.01616) inspo for experiments/contributions character etc? idk
## next
- gpu setup