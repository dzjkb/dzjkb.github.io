the NaNs what is this my g
### histograms
ok how about saving audio values manually?
and logging min/max/mean in tensorboard

another manual fix, `Image.LANCZOS` instead of `Image.ANTIALIAS` (see [this SO answer](https://stackoverflow.com/questions/76616042/attributeerror-module-pil-image-has-no-attribute-antialias))
ok the seaborn histograms are also expensive, log every n epochs?
or just don't log histograms - can do it on selected epochs later if actually needed
### nans
`torch.autograd.set_detect_anomaly(True)` ([docs](https://docs.pytorch.org/docs/1.12/autograd.html#torch.autograd.set_detect_anomaly))
running 600 epochs again

ok this line throws the NaN
```
return torch.pow(x - y, 2).mean() / torch.pow(y, 2).mean()

RuntimeError: Function 'DivBackward0' returned nan values in its 0th output.
```
### btw
- [x] make noise configurable
- [x] make amp mod configurable