## experiments
[[experiments - vctk words]]

ok note to self: 5e-4 lr is too large as the initial lr, everything falls apart
## rave repro
take original VCTK recordings and run [[RAVE]] ds preprocessing

installing rave in the same env gives:
```
 + acids-rave==2.3.1 (from file:///home/jp/mgr2024/RAVE)
 + blinker==1.9.0
 + flask==3.1.3
 + gin-config==0.5.0
 + gputil==1.4.0
 + iniconfig==2.3.0
 + itsdangerous==2.2.0
 + lmdb==2.2.0
 + nn-tilde==1.5.6
 + pluggy==1.6.0
 + pytest==9.0.3
 - pytorch-lightning==2.6.1
 + pytorch-lightning==1.9.0
 - scipy==1.15.3
 + scipy==1.10.0
 + udls==1.1.4
```
let's see if reinstalling lightning at 2.6.1 will break something

running
```
rave preprocess --input_path ./vctk_words_v2/audio --output_path ./vctk_rave --channels 2 --sampling_rate 48000 --num_signal 72000
```
or actually `--num_signal 4800` as that's the `0.1s` minimal sample length
ok works?

now
```
rave train --config configs/jpconfig.gin --db_path /media/Data/jp_dir/sounds/vctk_rave --out_path /media/Data/jp_dir/sounds/rave_training --name vctk_words_v0 --channels 1 --workers 4 --val_every 4000 --max_steps 3000000 --gpu 1 --n_signal 4800
```

yep major version upgrade changes the callback mechanisms, need to create a separate env eh
and degrade setuptools to `81.0.0` bc of `pkg_resources` being deprecated
ok done

ok apparently the `n_signal` not divisible by compression ratio might be a problem? omg
ok try `4*4*4*2*16*3 = 6144` samples (`--n_signal`)
doesn't work wtf man
0 element tensors, `--n_signal` > n_samples in some of the samples, that makes sense probably?
need to do `--n_signal 4096` jeeeeeeesus, that's like 2 latent vectors per training samples but ok?

```
  | Name                     | Type                  | Params
  -------------------------------------------------------------------
  0 | pqmf                     | CachedPQMF            | 16.7 K
  1 | encoder                  | VariationalEncoder    | 16.1 M
  2 | decoder                  | GeneratorV2           | 15.5 M
  3 | discriminator            | DescriptDiscriminator | 42.5 M
  4 | audio_distance           | AudioDistanceV1       | 0
  5 | multiband_audio_distance | AudioDistanceV1       | 0
  -------------------------------------------------------------------
  74.1 M    Trainable params
  0         Non-trainable params
  74.1 M    Total params
  296.290   Total estimated model params size (MB)       
```
this shit small