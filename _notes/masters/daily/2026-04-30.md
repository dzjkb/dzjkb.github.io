## experiments
[[experiments - vctk words]]

yeah sure it's kind of working but it's slow af
let it run a bit more why not
## rave repro
pad the ds
```
ffmpeg -i in.wav -af "apad=whole_dur=1" out.wav
```
and use `n_signal=32768`
whole dir:
```
find audio/ -type f \( -name '*.wav' -o -name '*.flac' \) -print0 | \
    xargs -0 -n1 -P8 -I{} sh -c '
      f="$1"; rel="${f#audio/}"; o="audio_padded_1s/$rel"
      mkdir -p "$(dirname "$o")"
      ffmpeg -nostdin -loglevel error -y -i "$f" -af "apad=whole_dur=1" "$o"
    ' _ {}  
```
sanity check: `ffprobe -show_streams out.wav | grep duration`

and then
```
rave preprocess --input_path ./vctk_words_v2/audio_padded_1s --output_path ./vctk_rave --channels 1 --sampling_rate 48000 --num_signal 24000

rave train --config configs/jpconfig.gin --db_path /media/Data/jp_dir/sounds/vctk_rave --out_path /media/Data/jp_dir/sounds/rave_training --name vctk_words_v0 --channels 1 --workers 4 --val_every 4000 --max_steps 3000000 --gpu 1 --n_signal 32768
```

bro what is this magic
`strace -p 1948477 -e trace=getdents64,openat -f 2>&1 | head -50`
damn claude u good
ok changed `--num_signal` to 24000, should work, training on halfwords won't matter until training a prior

training on a lot of silence, not sure that's a problem?

`valid_signal_crop=False` - this cuts out the whole sample in practice since the receptive field is like ~0.5s
- [ ] why does [[RAVE]] do `valid_signal_crop` exactly?  [frozen:: true]

ok running
## local latex
tbd
## next
aaaaaaaaaaaaaaa
- evaluation suite on latest [[VCTK]] models
- [x] local latex setup in vscode - apparently the LaTeX workshop extension can replicate overleaf
- get gpu running somehow