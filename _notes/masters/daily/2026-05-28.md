## experiments
nf - [[experiments - vctk words]]
no latent loss warmup prevents `nan`s even without gradient clipping mmhm
mode collapse, oops - decrease latent weight to `1e-5`
ok `nan`s, try `2e-5`
- why tf don't I decrease the training set size by half, at least for these initial experiment stages
ok mode collapse again, sth between `1e-5` and `2e-5`????
doesn't make sense, try architectural - maybe go back to `tanh`?
- with a higher rather than lower latent loss weight, the more flexible posterior should be able to give better reconstructions while fitting the prior better
mode collapse again, RIP
you sure everything ok with the nf latent loss?

what if I run `percussive2` in parallel on AWS ok ok ok
- assuming I do half the steps (ds is 10x smaller than vctk) and it's almost 2x faster I get ~18h for phase 1
maybe I should get the whole eval procedure figured out first though
## evals
more metrics + get going with visualizations?

what's the moment I actually start writing the thesis
if I get the NF reproductions/uncond. generations that sound better
and then I do metrics+viz's
## rave repro
prior training once the run is complete
## thesis writing
I need to organize all my references goddamn
## next
- create the third last dataset - SFX or sum shi, from freesound?
- experiments - NF, [[RAVE]] prio
- ask the seminar guy/mamodrzejewski for direction
- thesis start planning out chapters
- gpu setup yeahhh idk