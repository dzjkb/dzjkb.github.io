~ haven't felt like this in a long time
~ dreadful waking up
## experiments
#### vctk words
still following the `version_13` curve identically, nice, phase2 is where it's at
- it's learning slower despite a smaller param count, interesting
- latent loss way lower, better generations???
#### percussive2
bumped fm loss to `50.0` lmao, final lr to `7e-7`
losses oscillating with no visible trend, but it's actually learning something, albeit slowly
reconstructions are starting to sound reasonable

running again with fm `50.0` but lr `1e-5`
how tf is this behaving the same way with `50.0` fm loss
ok the lr is the same though something's screwed up fck
- yeah it gets loaded from ckpt, not re-initialized
- I need to do manual checkpoint patching again fml
restarting
- right I changed the scheduler to constant, but the LR values are still there
- patch again
```
checkpoint = torch.load("phase1.ckpt", map_location="cpu")
new_lr = 1e-4

for opt_state in checkpoint["optimizer_states"]:
    for param_group in opt_state["param_groups"]:
        param_group["lr"] = new_lr

torch.save(checkpoint, "phase2.ckpt")
```

ok need to move this to gpu2, ~120pln on AWS this month probs
overriding `strict=False` for state dict load
running
## evals
vctk words labels in [[VCTK]]
ok got em

what models for eval do I actually have - the `version_13/14` (`v8.yaml`) one or `version_15` + continuations (`v9.yaml`)
- take the first one
ok got the labels got the encodings got the notebook let's go

also - what was the [[RAVE]] "effective dimensionality" thing?
also - I should calculate the fidelity of the latent space
## rave repro
running prior training
what scheme to mimic fixed length generation here?
- option 1: just generate short clips
- option 2: train on 1s clips rather than the full dataset; I feel like this is going to break something in the training though for sure

ok done, got a uncond generation script thx Claude
ok the clips actually sound pretty nice, almost like words
definitely voice-like, nice, I can do evals on this

I should probably explore the second prior generation option to be more comparable
## thesis writing
organize references?

@mamodrzejewski Qs:
- I have this set of models/datasets and this set of evaluations, what do you think?
## next
- create the third last dataset - SFX or sum shi, from freesound?
- ask the seminar guy/mamodrzejewski for direction
- thesis start planning out chapters
- gpu setup yeahhh idk